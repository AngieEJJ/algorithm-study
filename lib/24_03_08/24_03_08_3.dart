//Q. Find the first non-consecutive number

int? firstNonConsecutive(List<int> arr) {
  for (int i = 0 ; i < arr.length; i++ ) {
    if (arr[i+1] - arr[i] == 1.abs()) {
    }
    return null;
  }
  return null;
}